### Reconstructed (was missing -- file had been overwritten by Cart+CartItem).
### Referenced as `status: OrderStatus, default=OrderStatus.pending` in Order,
### and as `status text NOT NULL DEFAULT 'pending'` in the shopping migration SQL.
### Lifecycle matches the customer app's Orders feature list.
enum: OrderStatus
serialized: byName
values:
  - pending
  - processing
  - shipped
  - delivered
  - cancelled
  - returned
