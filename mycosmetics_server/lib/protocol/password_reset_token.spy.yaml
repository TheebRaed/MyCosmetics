### Returned to client after successful auth. Never contains passwordHash.
class: AuthUser
fields:
  id: int
  email: String
  fullName: String
  role: UserRole
  avatarUrl: String?

class: AuthResult
fields:
  user: AuthUser
  token: String
