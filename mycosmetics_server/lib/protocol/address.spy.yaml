### Reconstructed (was missing -- content had been overwritten by SkinProfile).
### Referenced as relation(parent=addresses...) from Order, and as
### shippingAddress: Address in the OrderDetail read-model DTO.
class: Address
table: addresses
fields:
  userId: int, relation(parent=users, onDelete=Cascade)
  fullName: String
  phone: String
  line1: String
  line2: String?
  city: String
  state: String?
  postalCode: String?
  country: String
  isDefault: bool, default=false
  createdAt: DateTime
  updatedAt: DateTime
indexes:
  addresses_user_idx:
    fields: userId
